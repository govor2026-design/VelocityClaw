version: "3.9"
services:
  velocity_claw:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./workspace:/workspace
    restart: always
    user: velocity_claw
    command: ["python", "cli.py", "--server"]
